import Window from './Window';

const Sonification = ({ anchor, track }) => {
  return (
    <Window anchor={ anchor } title={ `${ track }: Sonification Settings` }>

    </Window>
  );
};

export default Sonification;