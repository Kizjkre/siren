import Window from '../../Window';

const FileInfo = ({ anchor, filename }) => {
  return (
    <Window anchor={ anchor } title={ `File Info for ${ filename }` }>

    </Window>
  );
};

export default FileInfo;
