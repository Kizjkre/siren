import IndexSectionTemplate from './IndexSectionTemplate';

const IndexTutorial = ({ offset }) => {
  return (
    <IndexSectionTemplate offset={ offset } title="Usage" color="danger">
      <div className="box">

      </div>
    </IndexSectionTemplate>
  );
};

export default IndexTutorial;
